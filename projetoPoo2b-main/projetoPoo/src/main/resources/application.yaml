
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/PobreFlix?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: aluno
    password: aluno
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
